# 🔧 Troubleshooting Guide

## 📋 สารบัญ

1. [Pull Code vs Pull Request](#pull-code-vs-pull-request)
2. [ปัญหา Sync](#ปัญหา-sync)
3. [ปัญหาที่พบบ่อย](#ปัญหาที่พบบ่อย)
4. [FAQ](#faq)

---

## 🔄 Pull Code vs Pull Request

### Pull Code (ดึงโค้ด)

**คืออะไร:**
```
Pull Code = ดึงโค้ดล่าสุดจาก GitHub มาที่เครื่องคุณ
```

**ใช้เมื่อไหร่:**
```
✅ ก่อนเริ่มทำงานใหม่
✅ ก่อน push
✅ เมื่อต้องการ code ล่าสุด
```

**คำสั่ง:**
```bash
git pull origin staging
```

**ผลลัพธ์:**
```
- โค้ดในเครื่องคุณอัพเดทเป็นเวอร์ชันล่าสุด
- ได้งานที่คนอื่นทำไว้
- ไม่มีการ merge หรือ review
```

**เปรียบเทียบ:**
```
Pull Code เหมือน:
- ดาวน์โหลดไฟล์ล่าสุด
- ซิงค์ข้อมูล
- อัพเดทโค้ด
```

---

### Pull Request (PR)

**คืออะไร:**
```
Pull Request = ขอให้รวมงานของคุณเข้า staging
```

**ใช้เมื่อไหร่:**
```
✅ เมื่อทำงานเสร็จแล้ว
✅ ต้องการให้คนอื่น review
✅ พร้อม merge เข้า staging
```

**วิธีสร้าง:**
```
1. Push branch ขึ้น GitHub
2. ไป GitHub
3. คลิก "Compare & pull request"
4. กรอกข้อมูล
5. Create pull request
```

**ผลลัพธ์:**
```
- สร้างคำขอให้ merge งานของคุณ
- คนอื่นเห็นว่าคุณแก้อะไร
- ต้องผ่าน review ก่อน merge
- มี discussion และ comments
```

**เปรียบเทียบ:**
```
Pull Request เหมือน:
- ส่งงานให้หัวหน้าตรวจ
- ขออนุมัติ
- รอ feedback
```

---

### สรุปความแตกต่าง

| | Pull Code | Pull Request |
|---|---|---|
| **ภาษาอังกฤษ** | git pull | Pull Request (PR) |
| **ทำอะไร** | ดึงโค้ดมา | ขอ merge โค้ดเข้า |
| **ทิศทาง** | GitHub → เครื่องคุณ | เครื่องคุณ → GitHub |
| **ใช้เมื่อไหร่** | ก่อนเริ่มงาน | หลังทำงานเสร็จ |
| **คำสั่ง** | `git pull` | สร้างบน GitHub |
| **Review** | ไม่มี | มี |
| **Merge** | อัตโนมัติ | ต้องได้รับ approval |

---

### ตัวอย่างการใช้งาน

**Scenario 1: เริ่มทำงานใหม่**
```bash
# ใช้ Pull Code
git checkout staging
git pull origin staging  ← ดึงโค้ดล่าสุด
git checkout -b feature/my-work
```

**Scenario 2: ทำงานเสร็จแล้ว**
```bash
# Push ก่อน
git push origin feature/my-work

# จากนั้นสร้าง Pull Request บน GitHub
# ไม่ใช่ git pull!
```

**Scenario 3: ก่อน Push**
```bash
# ใช้ Pull Code อีกครั้ง
git checkout staging
git pull origin staging  ← ดึงงานคนอื่นมา
git checkout feature/my-work
git merge staging
git push origin feature/my-work
```

---

## 🔄 ปัญหา Sync

### ปัญหา: Code ไม่ Sync

**สถานการณ์:**
```
John ทำงาน 4 ชั่วโมง ใช้ code เก่า
Jane ทำเสร็จก่อน merge ไปแล้ว
John push → ไม่มีงานของ Jane!
```

**สาเหตุ:**
```
❌ John ไม่ได้ pull ก่อน push
❌ John ใช้ code เก่าตอนเริ่มทำงาน
❌ Jane merge ไปแล้ว แต่ John ไม่รู้
```

**วิธีแก้:**
```bash
# ก่อน push ทุกครั้ง ต้อง pull ก่อน!

# 1. Pull staging ล่าสุด
git checkout staging
git pull origin staging

# 2. Merge เข้า branch ของตัวเอง
git checkout feature/my-work
git merge staging

# 3. แก้ conflict (ถ้ามี)
# 4. Push
git push origin feature/my-work
```

---

### วิธีป้องกัน

**1. Pull ก่อนเริ่มงานเสมอ**
```bash
# ทุกครั้งที่เริ่มทำงานใหม่
git checkout staging
git pull origin staging
git checkout -b feature/new-work
```

**2. Pull ก่อน Push เสมอ**
```bash
# ก่อน push
git checkout staging
git pull origin staging
git checkout feature/my-work
git merge staging
git push origin feature/my-work
```

**3. ใช้ Script ช่วย (แนะนำ!)**
```bash
# Script จะทำ pull + merge + push อัตโนมัติ
./scripts/safe-push.sh
```

---

### Safe Push Script

**วิธีใช้:**
```bash
# แทนที่จะใช้
git push origin feature/my-work

# ใช้ script นี้แทน
./scripts/safe-push.sh
```

**Script จะทำให้:**
```
1. ✅ Pull staging ล่าสุด
2. ✅ Merge เข้า branch ของคุณ
3. ✅ เช็ค conflict
4. ✅ Push ขึ้น GitHub
5. ✅ ป้องกัน code ไม่ sync
```

---

## ⚠️ ปัญหาที่พบบ่อย

### 1. Push ไม่ได้

**Error:**
```
! [rejected] feature/my-work -> feature/my-work (non-fast-forward)
```

**สาเหตุ:**
- คนอื่น push ไปก่อนแล้ว
- Branch ไม่ up-to-date

**วิธีแก้:**
```bash
# Pull ก่อน
git pull origin staging
git merge staging
# แก้ conflict (ถ้ามี)
git push origin feature/my-work
```

---

### 2. Conflict

**Error:**
```
CONFLICT (content): Merge conflict in shared/utils.js
```

**สาเหตุ:**
- คุณและคนอื่นแก้ไฟล์เดียวกัน
- Git ไม่รู้ว่าจะเอาโค้ดของใคร

**วิธีแก้:**
```bash
# 1. เปิดไฟล์ที่ conflict
# 2. หา conflict markers:
<<<<<<< HEAD
// โค้ดของคุณ
=======
// โค้ดของคนอื่น
>>>>>>> staging

# 3. แก้ไขให้เหลือโค้ดที่ถูกต้อง
# 4. ลบ conflict markers
# 5. Commit
git add .
git commit -m "resolve: merge conflict"
git push origin feature/my-work
```

---

### 3. ลืมสร้าง Branch

**ปัญหา:**
```
แก้โค้ดไปแล้ว แต่ลืมสร้าง branch
ยังอยู่ใน staging branch!
```

**วิธีแก้:**
```bash
# ย้ายการแก้ไข
git stash                    # เก็บการแก้ไข
git checkout -b feature/my-work  # สร้าง branch ใหม่
git stash pop               # นำการแก้ไขกลับมา
```

---

### 4. Push ผิด Branch

**ปัญหา:**
```
Push ไปที่ branch ผิด
```

**วิธีแก้:**
```bash
# ลบ branch บน GitHub
git push origin --delete wrong-branch

# สร้างใหม่
git checkout -b correct-branch
git push origin correct-branch
```

---

### 5. Commit ผิด

**ปัญหา:**
```
Commit แล้วแต่ยังไม่ push
ต้องการแก้ไข commit message
```

**วิธีแก้:**
```bash
# แก้ commit message ล่าสุด
git commit --amend -m "new message"

# ถ้า push ไปแล้ว
git push origin feature/my-work --force
```

---

### 6. แก้ไฟล์ผิด

**ปัญหา:**
```
แก้ไฟล์ที่ไม่ควรแก้
ยังไม่ได้ commit
```

**วิธีแก้:**
```bash
# ยกเลิกการแก้ไขไฟล์เดียว
git checkout -- filename.js

# ยกเลิกการแก้ไขทั้งหมด
git reset --hard
```

---

### 7. Branch Protection

**Error:**
```
remote: error: GH006: Protected branch update failed
```

**สาเหตุ:**
- พยายาม push ตรงเข้า staging
- Branch มี protection

**วิธีแก้:**
```bash
# ห้าม push ตรงเข้า staging!
# ต้องสร้าง PR แทน

# 1. สร้าง feature branch
git checkout -b feature/my-work

# 2. Push feature branch
git push origin feature/my-work

# 3. สร้าง PR บน GitHub
```

---

### 8. PR ถูก Reject

**ปัญหา:**
```
Reviewer ขอให้แก้ไข
```

**วิธีแก้:**
```bash
# 1. แก้ไขตามที่ reviewer บอก
# 2. Commit และ push อีกครั้ง
git add .
git commit -m "fix: address review comments"
git push origin feature/my-work

# PR จะอัพเดทอัตโนมัติ
# ไม่ต้องสร้าง PR ใหม่
```

---

### 9. ลบ Branch ไม่ได้

**Error:**
```
error: Cannot delete branch 'feature/my-work' checked out at...
```

**สาเหตุ:**
- กำลังอยู่ใน branch ที่จะลบ

**วิธีแก้:**
```bash
# ย้ายไป branch อื่นก่อน
git checkout staging
git branch -d feature/my-work
```

---

### 10. Merge Conflict ซับซ้อน

**ปัญหา:**
```
Conflict หลายไฟล์ แก้ไม่ถูก
```

**วิธีแก้:**
```bash
# Option 1: ยกเลิก merge
git merge --abort

# Option 2: ใช้ tool ช่วย
git mergetool

# Option 3: ถามคนที่แก้ไฟล์เดียวกัน
"John, เราแก้ไฟล์เดียวกัน ขอประสานหน่อย"
```

---

### 11. Lost Commits

**ปัญหา:**
```
Commit หายไป!
```

**วิธีแก้:**
```bash
# ดู history ทั้งหมด
git reflog

# กู้คืน commit
git cherry-pick <commit-hash>
```

---

### 12. Wrong Base Branch

**ปัญหา:**
```
สร้าง branch จาก main แทน staging
```

**วิธีแก้:**
```bash
# Rebase ไปที่ staging
git checkout feature/my-work
git rebase staging
```

---

### 13. Accidentally Deleted Files

**ปัญหา:**
```
ลบไฟล์โดยไม่ตั้งใจ
```

**วิธีแก้:**
```bash
# กู้คืนไฟล์
git checkout HEAD -- filename.js

# กู้คืนทั้งหมด
git reset --hard HEAD
```

---

## ❓ FAQ

### Q: ต้อง pull ทุกครั้งหรือไม่?

**A:** ใช่! Pull ก่อนเริ่มงานและก่อน push เสมอ

```bash
# ก่อนเริ่มงาน
git pull origin staging

# ก่อน push
git pull origin staging
```

---

### Q: ถ้าลืม pull จะเกิดอะไรขึ้น?

**A:** Code ไม่ sync, อาจเกิด conflict

```bash
# แก้ได้ โดย pull ทีหลัง
git pull origin staging
git merge staging
```

---

### Q: PR คืออะไร?

**A:** Pull Request = คำขอให้ merge งานของคุณเข้า staging

```
PR ≠ Pull Code
PR = ขอ merge
```

---

### Q: ทำไมต้องใช้ PR?

**A:** เพื่อ:
- ✅ Review code ก่อน merge
- ✅ ป้องกันโค้ดผิดพลาด
- ✅ มี history ชัดเจน
- ✅ ทีมรู้ว่าใครทำอะไร

---

### Q: Conflict คืออะไร?

**A:** เกิดเมื่อคุณและคนอื่นแก้ไฟล์เดียวกัน

```
Git ไม่รู้ว่าจะเอาโค้ดของใคร
→ ต้องแก้ manual
```

---

### Q: วิธีป้องกัน Conflict?

**A:**
```
1. แบ่งงานชัดเจน (แต่ละคนแก้ไฟล์ต่างกัน)
2. Pull บ่อยๆ
3. Commit เล็กๆ บ่อยๆ
4. ประสานกันถ้าต้องแก้ไฟล์เดียวกัน
```

---

### Q: Branch คืออะไร?

**A:** ห้องทำงานส่วนตัว

```
Branch = สำเนาของโค้ดที่คุณจะทำงานแยกจากคนอื่น
- แก้ไขอะไรก็ได้ ไม่กระทบคนอื่น
- เหมือนห้องทำงานส่วนตัว
```

---

### Q: ทำไมต้องใช้ Branch?

**A:**
```
✅ ทำงานแยกจากคนอื่น
✅ ไม่กระทบ staging
✅ ทดสอบได้อิสระ
✅ ลบได้ถ้าไม่ต้องการ
```

---

### Q: Staging คืออะไร?

**A:** กองกลางที่รวมงานของทุกคน

```
Staging = Branch หลักที่ทุกคนใช้ร่วมกัน
- มีโค้ดล่าสุดของทีม
- ทุกคน pull จาก staging
- ทุกคน merge เข้า staging
```

---

### Q: Main คืออะไร?

**A:** Production branch

```
Main = โค้ดที่ใช้งานจริง (Production)
- ห้าม push ตรง
- Merge จาก staging เท่านั้น
- Deploy manual
```

---

### Q: ใช้ Script หรือไม่?

**A:** แนะนำให้ใช้!

```bash
# ใช้ script ช่วย
./scripts/safe-push.sh
./scripts/check-before-commit.sh

# ทำให้ชีวิตง่ายขึ้น
# ป้องกันผิดพลาด
```

---

### Q: ถ้าไม่แน่ใจจะทำยังไง?

**A:** ถาม!

```
❓ ไม่เข้าใจ → ถาม
❓ ไม่แน่ใจ → ถาม
❓ เจอปัญหา → ถาม

ทุกคนเคยเป็นมือใหม่มาก่อน
```

---

## 🔑 API & CORS Issues

### ปัญหา: 403 Forbidden - Invalid API Key

**สาเหตุ:** API Key ไม่ถูกต้อง

**วิธีแก้:**
1. ไปที่ [NoCodeBackend.com](https://nocodebackend.com)
2. Login → เลือกโปรเจกต์ `54566_tourwow`
3. Settings → API Keys → Copy API Key
4. แก้ไขไฟล์ `config.js`:
```javascript
API_KEY: 'YOUR_ACTUAL_API_KEY_HERE'
```

### ปัญหา: CORS Error

**สาเหตุ:** Domain ไม่ได้รับอนุญาต

**วิธีแก้:**
1. เข้า NoCodeBackend Dashboard
2. Settings → Secret Keys
3. หาส่วน **Allowed domains**
4. ใส่ `*` (development) หรือ domain ของคุณ
5. Save

**ค่าที่แนะนำ:**
- Development: `*` หรือ `localhost, 127.0.0.1`
- Production: `yourdomain.com, *.yourdomain.com`

**ยังไม่ได้?**
- รอ 1-2 นาที (ระบบอัพเดท)
- Clear browser cache
- ลอง browser อื่น
- ตรวจสอบ API Key อีกครั้ง

---

## 📞 ติดต่อ & ช่วยเหลือ

**ปัญหาทั่วไป:**
- อ่าน WORKFLOW.md
- อ่าน TROUBLESHOOTING.md (ไฟล์นี้)

**Code Ownership:**
- อ่าน CODEOWNERS.md
- ใช้ `./scripts/who-owns.sh`

**API/CORS Issues:**
- ตรวจสอบ API Key ใน config.js
- ตรวจสอบ CORS settings ใน NoCodeBackend
- Email: support@nocodebackend.com

**อื่นๆ:**
- ถามใน team chat
- ถาม team lead

---

**สร้างโดย:** Vibe Code Team  
**อัพเดทล่าสุด:** 2 มกราคม 2026  
**เวอร์ชัน:** 2.1
