# 📅 Date Format Clarification Request

## 📋 สรุป
ขอชี้แจงเกี่ยวกับรูปแบบวันที่ที่ต้องการใช้งานจริง

## 🎯 รูปแบบที่ต้องการ (Updated)

### ❌ เดิม (ไม่ถูกต้อง)
```
numeric_short → "09/69" (MM/YY - ปีย่อ 2 หลัก)
```

### ✅ ใหม่ (ถูกต้อง)
```
MM/YYYY → "09/2569" (เดือน/ปีเต็ม 4 หลัก)
```

---

## 🔍 คำถาม

**Backend มี format `MM/YYYY` (เดือน + ปีเต็ม 4 หลัก) หรือไม่?**

### Option 1: ถ้ามีอยู่แล้ว
กรุณาแจ้ง format name ที่ต้องส่งใน `date_format` parameter

**ตัวอย่าง:**
```bash
GET /api/reports/by-travel-date?date_format=<format_name>
Response: "travel_month_label": "09/2569"
```

### Option 2: ถ้ายังไม่มี
กรุณาเพิ่ม format ใหม่ชื่อ `numeric_month_year_full`:

```typescript
// Format: MM/YYYY (เดือน/ปีเต็ม พ.ศ.)
numeric_month_year_full: (monthString: string) => {
    const [year, month] = monthString.split('-');
    const buddhistYear = parseInt(year) + 543;
    return `${month}/${buddhistYear}`;
}

// ตัวอย่าง:
// "2025-01" → "01/2568"
// "2024-12" → "12/2567"
// "2025-09" → "09/2569"
```

---

## 📊 สรุปรูปแบบที่ใช้งานจริง

| Tab | Format ที่ต้องการ | ตัวอย่าง | Format Name |
|-----|-------------------|----------|-------------|
| ตามวันเดินทาง | MM/YYYY | 09/2569 | `numeric_month_year_full` หรือ ? |
| ตามวันจอง | MM/YYYY | 12/2567 | `numeric_month_year_full` หรือ ? |
| ช่วงเวลาจอง | DD/MM/YYYY | 01/09/2569 | `numeric_full` (มีอยู่แล้ว) |

---

## ⚠️ ความเร่งด่วน
- **Priority:** HIGH
- **Reason:** Frontend ใช้ `numeric_short` ผิด (ได้ 09/69 แทน 09/2569)
- **Impact:** ผู้ใช้เห็นปีย่อ 2 หลัก แทนที่จะเป็นปีเต็ม 4 หลัก

---

## 📞 กรุณาตอบกลับ
1. Backend มี format `MM/YYYY` อยู่แล้วหรือไม่?
2. ถ้ามี ชื่อ format คืออะไร?
3. ถ้าไม่มี ใช้เวลาเพิ่มนานแค่ไหน?

**Created:** 2025-01-16  
**Reporter:** Frontend Team  
**Status:** 🔴 Urgent - Waiting for Response
